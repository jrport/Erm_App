<header class='max-2xl:hidden h-[7dvh] flex gap-2 p-4 font-semibold text-xl text-black items-center  bg-stone-400 '>
  <% links.each do |link| %>
    <%= link_to link[:title], link[:uri] %> \
  <% end %>
</header>
